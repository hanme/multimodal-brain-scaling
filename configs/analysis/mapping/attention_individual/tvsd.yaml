experiment_name: mapping-tvsd-attention_individual
base_config: ../mapping-base.yaml

data_filters:
    set_filters:
        benchmark_name:
            - tvsd

        probe_type:
            - attention_individual


experiment_description: >
    ""